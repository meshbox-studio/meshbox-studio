defaultPlatforms:
  - linux/arm64
  - linux/amd64

builds:
  - id: meshbox-studio
    main: ./cmd/meshbox-studio
    env:
      - CGO_ENABLED=0
    flags:
      - -trimpath
    ldflags:
      - -s
      - -w
