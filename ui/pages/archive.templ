package pages

import "github.com/meshbox-studio/meshbox-studio/ui/layouts"

templ Archive() {
	@layouts.BaseLayout("/archive") {
		<div class="flex h-full items-center justify-center px-4">
			<div class="w-full max-w-2xl rounded-xl border bg-card p-8 text-center shadow-sm">
				<h1 class="text-3xl font-bold tracking-tight">Archive</h1>
				<p class="mt-3 text-muted-foreground">Placeholder: this is the /archive page.</p>
			</div>
		</div>
	}
}
